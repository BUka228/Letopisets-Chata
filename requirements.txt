# Telegram Bot Library (Версия от пользователя, добавим [ext] на всякий случай, хотя apscheduler ниже)
python-telegram-bot[ext]==22.0

# Для загрузки .env файлов
python-dotenv==1.1.0

# Для работы с часовыми поясами (версия 2025.2 исправлена на актуальную)
pytz==2024.1

# Для асинхронных HTTP-запросов к прокси Cloudflare Worker
httpx>=0.25.0,<0.28.0

# Зависимость для JobQueue в python-telegram-bot v21+
